# 2026-09-15 - git-guard read an unlisted global flag as the subcommand

ts: 2026-09-15T01:10:32Z
commit: 8f91906
session: 44a40c81-affc-4987-b881-bf7aec377496
status: verified
fact: hooks/git-guard.mjs skipped only a fixed list of boolean global options before the subcommand (-p, --paginate, --no-pager, --bare, --no-replace-objects, the four pathspec flags, --no-optional-locks), so any other global - `-P`, `--no-advice`, `--no-lazy-fetch`, `--config-env`, `--attr-source` - was read as the subcommand and the verb behind it was never checked: `git -P push` was allowed by the shipped hook at 8f91906. git 2.49's own usage lists -P, --no-lazy-fetch and --no-advice as globals, and git accepts `--config-env <name>=<envvar>` and `--attr-source <tree-ish>` in separate form. Fixed by skipping any unlisted dash token before the subcommand (which can only over-block) and adding the two value-taking globals to VALUE_FLAGS. The same skeptic round showed the 2026-09-14 argv-array walk had lost writes the old whitespace re-split caught, wherever the hook's tokenizer differs from bash (an unquoted `$(...)` that bash word-splits, a `\"` inside double quotes); by operator ruling both views now vote, and either one naming a write blocks.
basis: `git -h` (git version 2.49.0.windows.1) lists `-P`, `--no-lazy-fetch`, `--no-advice` and `--config-env` among the globals; `git -P version`, `git --no-advice version`, `git --config-env user.x=HOME version` and `git --attr-source HEAD version` each print the version, so the flag is consumed as a global. Red-first with the final test file (81 tests): the argv-only hook -> 78 pass, 3 fail - 'an unlisted global flag does not hide the subcommand (2026-09-14)' on `git -P push`, 'a write the hook cannot tokenize like bash is still blocked by the re-split view (2026-09-14)' on `git -c $(echo push.default=current push) origin`, and the quoted-value twin's over-block assertion - at 2026-09-15T01:10:32Z; the shipped hook at 8f91906 -> 77 pass, 4 fail (unlisted flag, quoted value, merge-base, merge-* ruling; the re-split-view twin passes because the old view caught those). Final hook -> 81 pass, 0 fail; full suite 598 pass, 0 fail, at 01:14:13Z. decide() differentials, 8f91906 vs final: orchestrator table, 73 strings -> 7 to allow (merge-*), 0 to block; round-1 corpus, 30,340 strings -> 24,444 to allow, 0 to block, the only non-merge-* real subcommands among them `merge --abort`, `merge --squash` and the non-command `mergenx`; round-1 corpus, 21,331 strings -> 549 to allow, 0 to block, 2 non-merge-* (both `merge --abort` / `--squash`); round-2 corpus, 16,152 strings -> 264 to allow, 2,963 to block, the 30 unwrapped flips to allow all merge-* plumbing or `merge-now` / `merge-x` names, at 01:17:55Z; round-2 corpus with a bash shell-function argv oracle, 32,410 strings -> 2,366 to allow, 5,652 to block, at 01:16:41Z - no oracle-checked flip to allow reaches git as a history write (named merge-* plumbing, `merge --abort` / `--squash`, reads behind a quoted -C, `merge-x`, or no subcommand), its 2,047 wrapped flips were not oracle-checked, and its reads newly blocked are all the `git -C $(echo . V) V` shape (44 status, 44 log).
re-verify: node --test tests/git-guard.test.mjs
