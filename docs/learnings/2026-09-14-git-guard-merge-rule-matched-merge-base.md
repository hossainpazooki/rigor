# 2026-09-14 - git-guard merge rule matched merge-base

ts: 2026-09-14T22:53:58Z
commit: 8f91906
session: 44a40c81-affc-4987-b881-bf7aec377496
status: verified
fact: hooks/git-guard.mjs's merge-blocking rule was `/^git\s+merge\b(?!\s+(--abort|--squash)\b)/`; `\b` also matches between "merge" and a following hyphen, so the whole merge-* plumbing family was refused as if it were `git merge` - `git merge-base` (a read), `git merge-tree` (objects at most), and `git merge-file` / `merge-index` / `merge-one-file` / the `merge-<strategy>` helpers (working tree or index, never a ref). Found at pick-up 2026-09-14 when live Bash calls containing `git merge-base --is-ancestor cb2da49 HEAD` in ~/dev/site were blocked (22:10:31Z and 22:11:09Z). Two fixes were refuted before commit. (1) A whitespace-or-end lookahead `(?=\s|$)` let `git merge>out` - a real merge in bash - through; the orchestrator's old-vs-new decide() differential caught it. (2) The corrected rule `(?![\w-])` was then refuted by two skeptics: a quoted global-option value whose second word began `merge-` (`git -C "r merge-x" merge feature`) hid a real merge, because the hook re-split argv on whitespace before finding the subcommand - a separate shipped bypass with its own entry (2026-09-14-git-guard-quoted-global-value-shifted-the-subcommand). The rule `/^git\s+merge(?![\w-])(?!\s+(--abort|--squash)\b)/` is the one that stands; how the subcommand is found was changed again on 2026-09-15 (two-view parsing and a global-flag fix, entry 2026-09-15-git-guard-read-an-unlisted-global-flag-as-the-subcommand). A boundary fix is a two-sided claim: its twins must include the forms a looser boundary admits, not only the forms the bug refused. Alias bypass-by-name, pre-existing in class, now includes merge-* alias names and is an open closure (entry 2026-09-15-git-guard-cannot-see-through-a-git-alias).
basis: orchestrator's own runs 2026-09-14, test file as of 22:5xZ (78 tests): hook at 8f91906 -> 76 pass, 2 fail (the merge-base/merge-tree twin and the quoted-value twin) at 22:53:10Z; the 22:51Z working-tree hook -> 78 pass, 0 fail at 22:51:49Z; full `node --test` -> 595 pass, 0 fail. The glued-redirect twin failed alone against the refuted `(?=\s|$)` rule (77 tests, 76 pass) at 22:37:35Z. decide() differentials, 8f91906 vs the 22:5xZ tree: orchestrator table, 73 strings -> 7 flips to allow, all merge-*, 0 to block; skeptic corpus, 30,340 strings -> 24,444 flips to allow, 0 to block, and every non-merge-* real argv subcommand inside those flips is `merge --abort` (57), `merge --squash` (57) or the non-command `mergenx` (57); skeptic corpus, 21,331 strings -> 677 flips to allow, 0 to block, 2 with a non-merge-* real subcommand, both `merge --abort` / `--squash`. Live PreToolUse: `git merge-base --is-ancestor 13e51e8 HEAD` in ~/dev/rigor exited 0 once the rule was edited - the live hook reads the repo file, while the stale 0.1.0 plugin-cache copy still carries the old rule.
re-verify: node --test tests/git-guard.test.mjs
