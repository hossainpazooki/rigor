# 2026-09-22 - check-dispatch never bound the tier label to the receipt

ts: 2026-09-22T23:34:00Z
commit: e63f0e4
session: 2afcbd5b-e7b9-4d82-bfb2-476e4799e1af
status: verified
fact: check-dispatch's four classes read `dispatch_tier` and `verifier_model` as unrelated fields. Class 1 (fox-and-henhouse) and class 2 (floor) test the label; class 4 (silent downgrade) tests `requested === answered`. Nothing tied the label to the model the config assigns that tier, so a record `{dispatch_tier: "judgment", requested: "claude-sonnet-5", answered: "claude-sonnet-5", inferred_stakes: "high"}` on a floored node passed clean. The gate's own header says the orchestrator being checked writes these fields, so the hole was inside its stated threat model. Found by a cross-model review (Codex CLI, gpt-6-astra, 2026-09-22) working from the code alone with simulated records; six Claude skeptic rounds and 634 tests had not surfaced it. Fixed as class 5, unbound tier label: `requested` must equal the tier's model in the record's harness block (`harness: "codex"` -> `config.codex`; absent or `claude` -> top-level), fail-closed when the tier has no model. Consequence for old logs: backlog-settlement runs 4-6 and payment-loop run 1 name `claude-opus-4-8` as the mid tier, the pin in force from 42a5238 (2026-07-22) to af4284a (2026-08-22), so they go exit 0 -> 1 under today's config and exit 0 under the config of their day, passed as the CLI's second argument. The gate checks a log against a config; which config is the caller's claim.
basis: at 2026-09-22T23:32Z at e63f0e4, `node ../briefs/2026-09-22-rigor-review/reproduce.mjs .` (the reviewer's packet; its recorded sha256 of scripts/check-dispatch.mjs, b2114aeb..., matched the tree) reported dispatchCases `cheap-model-with-judgment-label accepted=true` with `correct-judgment-control accepted=true` and `explicit-cheap-label-control accepted=false` as controls. Twin `seed 5` in tests/dispatch-check.test.mjs RED pre-fix (`not ok 23`, 26 pass / 3 fail), green post-fix; suite 634 -> 640. Post-fix the packet reports `cheap-model-with-judgment-label accepted=false`, controls unchanged. `git show 42a5238:config/models.json | grep mid` -> `claude-opus-4-8`; check-dispatch on run-4/5/6 and payment run-1 with that file exits 0, with today's file exits 1 on exactly the `unbound tier label` reason.
re-verify: node --test tests/dispatch-check.test.mjs
