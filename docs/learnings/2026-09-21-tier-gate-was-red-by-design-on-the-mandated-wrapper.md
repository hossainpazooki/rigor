# 2026-09-21 - check-tier-placement was red by design on the wrapper orchestrate mandates

ts: 2026-09-21T15:05:00Z
commit: 9626f06
session: 3e2af12f-214c-4987-8b9a-9cf91e417bf4
status: verified
fact: check-tier-placement read the tier pin only at the `agent()` call site, so a pin forwarded through a helper was invisible to it. `orchestrate` guardrail 10 mandates a stall-retry wrapper of exactly that shape - `async function run(prompt, opts) { let r = await agent(prompt, opts); if (r == null) r = await agent(prompt, Object.assign({}, opts, { label: opts.label + ':retry' })) }` - so every fan-out written the way the skill requires was red by construction. Measured on the harvest of session cc40b6d1: all 8 Workflow scripts exited 1 with 2-3 "agent() call without a tier pin" warnings each, in three domain repos (datum, meridian, baseline), while the persisted run receipts showed the answering models were exactly the configured mid/build/judgment tiers - 8 silent-tier-collapse alarms, zero collapse. This is NOT a misfire: the gate's own header (scripts/check-tier-placement.mjs:17-19 at 9626f06) documented "cannot see through indirection (a helper that sets model: away from the call site)". It is a calibration defect, and the cost is the mirror of the always-green gate that learn-from-misfire warns about - a gate that is always red on the mandated pattern is a gate you learn to ignore, which kills orchestrate guardrail 7. Fixed by resolving ONE hop: when the options argument is a forwarded identifier (a bare `opts`, or `Object.assign({}, opts, {...})` adding no pin of its own), the pin is sought at the call sites of the enclosing helper. Resolution fails closed - an unresolvable forward, or one unpinned caller out of many, still warns - and stops at one hop, so a wrapper calling a wrapper still warns.
basis: red-first, against the unchanged gate at 9626f06, `node --test --test-reporter=tap tests/tier-placement.test.mjs` printed `not ok 15 - a pin forwarded through the stall-retry wrapper is seen (harvest cc40b6d1)`, `not ok 16 - the agentType fallback variant of the wrapper resolves through its callers` and `not ok 20 - a caller that hardcodes a model literal is still flagged through the wrapper`, with the three looser-boundary guards (unpinned caller still warns; one unpinned caller out of two still warns; an unresolvable forward still warns) already green and green throughout. After the fix all 20 pass and the full suite is 632/632 at that instant (634 later the same session, once the two `check-harvest` twins were added). On the ORIGINAL artifacts rather than the twins: the 8 real scripts under the cc40b6d1 session scratchpad went 8/8 exit 1 to 8/8 exit 0, and both shipped examples (fanout-build, execute-plan) stayed exit 0.
re-verify: node --test tests/tier-placement.test.mjs
