# ADR index

Status is the ADR's own header line — not a summary of how the work feels. Two columns are kept
apart on purpose: **Status** is what was *decided*, **As built** is what actually *exists in the
tree today*. An accepted decision with nothing built is a plan; saying so is the point
(`implemented-vs-planned`).

| # | Decision | Status | As built | Open |
|---|---|---|---|---|
| [0001](0001-vendor-the-rules.md) | Vendor the working-agreement rules into the plugin — self-contained beats elegant | **Accepted** 2026-06-25 | Built — `rules/` + `rules/PROVENANCE.md` (point-in-time copy of `~/.claude/rules/`); `session-start` injects them only when the host has none (no double-load) | Re-vendor when the source rules change; the copy is dated, not live |
| [0002](0002-dataeng-is-judgment-not-a-universal-gate.md) | Data-eng verification is judgment, not a universal validator — ship discipline, not content | **Accepted** 2026-07-02 | Built — the four data-eng skills + the check scripts. **No universal validator, deliberately** | Three of four skills still **origin-only** (VANTAGE); `data-quality-fail-closed` reached its first non-origin domain 2026-07-19 (1 of ≥2) |
| [0003](0003-repo-context-and-learnings-files.md) | Per-repo context (AGENTS.md canonical + CLAUDE.md stub) and anchored learnings records | **Accepted** 2026-07-08, amended **07-12** (folder layout) and **07-14** (capture-time anchoring) | Built — `docs/learnings/` + `docs/handoff/` kit, `check-learnings.mjs`, `handoff`/`pick-up` skill edits, cartographer emits the brief pair, rigor's own AGENTS.md | **1 domain, 1 logged misfire** — first non-origin use produced a record whose basis did not reproduce; gate hardened. Needs a 2nd adopting repo |
| [0004](0004-loop-chassis-rigor-conscience.md) | Loop chassis (STATE.md spine + append-only run log + L1/L2/never-L3 + budget kill switch), piloted on the backlog effort | **Accepted** 2026-07-08 · **pilot SETTLED 2026-07-14** | Built + evaluated — 3 runs / 2 sessions; all five success criteria met (evaluation table in the ADR). Chassis kept and generalized into the ADR-0003 kit | `check-runlog` **built 2026-07-22** (`9d5c2f5`) when the 4th hand-check arrived — 13 tests, clean on this effort's log (6 entries). **Residual found and closed same day, 2026-08-18** (operator ruling: supersession) — the gate gained a `supersedes:` record class, the red payment-loop entry was corrected by a superseding record (gate green), `fanout-loop` step 5 now names the gate, and the incident is pinned (first closure-ledger record) |
| [0005](0005-wap-composition-and-catalog-drift.md) | WAP is the chassis at pipeline scale, not the conscience; the standing catalog drifts | **Settled (scoped)** 2026-07-19 — criterion 1 satisfied on the operator's two-domain basis (addenda 1–3); Proposed 2026-07-09 | Built — `docs/wap-bridge.md` (the earned bridge) + cross-links in the four data-eng skills; both firing entries in `docs/feedback/` (VANTAGE origin exemplar; CLDD non-origin adjudication, verdict **clarified**) | ① Resolution 2's standing-catalog sweep: designed, **not opened** — settlement does not open it. ② Same-operator caveat on both domains. ③ `data-quality-fail-closed` at 1 of ≥2 promotion domains |
| [0006](0006-silent-tier-collapse.md) | Silent tier collapse — an unpinned `agent()` call inherits the session model, so a fan-out can look like a specialized swarm while the session model silently does all the work; mechanize the build-tier pin check | **Accepted** 2026-07-18 (Proposed 07-15); both pending calls resolved — placement: separate gate; tic finding independently re-verified, mechanism clause corrected (`agentType:` alone is not a pin) | Built — `check-tier-placement.mjs` + 12 tests (criterion 1 met **red on the real tic collapse script**), `check-dispatch` worker-receipt class, `example.mjs` config-sourced tiers (fail-closed) + `model` receipts, skill prose updated | Criterion 2 **answered YES 2026-07-19** (VANTAGE Gate B fan-out + rigor's CLDD runs — all receipts named the pinned tier; `build == cheap` caveat). Open: gate-side receipt normalization (display-name echoes false-positive check-dispatch) |
| [0007](0007-mid-tier-opus.md) | A mid tier (Claude Opus, now **Opus 5**) between build and judgment — integration closer, `skeptic-verifier-fast`, and delegated contract authors run it; builders and floors unchanged | **Accepted** 2026-07-22 · **amended 2026-08-22** (re-pinned to `claude-opus-5`, operator-directed) | Built — config + `fallback_order`, both agent repins (tier-sync watched red→green), `check-dispatch` accepts `"mid"` (+3 red-first tests), prose synced across 6 files; **re-pinned 2026-08-22** in config + both agent frontmatters, tier-sync green | Usage-economics premise is still operator-reported, not measured (on Opus 4.8 or Opus 5). **Amended 2026-08-18:** the mid *tier* has run (backlog runs 4–6 + payment run-1 vote stage, receipts three-way, `check-dispatch` clean); the *agent* `skeptic-verifier-fast` still has not — no verdict record anywhere carries an `agentType` |
| [0008](0008-fanout-loop.md) | fanout-loop command: one invocation = one tier-laddered loop iteration; recurrence belongs to the host loop; standing L1 go per iteration with a per-instantiation ceiling | **Accepted** 2026-07-22 | Built — `commands/fanout-loop.md` + `check-dispatch` receipt normalization (`receiptMatches`, red-first). `check-runlog` deliberately deferred to run 4 (ADR-0004's own condition) | **Both discharged.** First instantiation executed — backlog-settlement runs 4–6 (2026-07-22 → 08-08), two honest negatives, cap raised to 250k after the run-5 breach; mid tier dispatched live in all three. Second instantiation (payment-loop-randomized, run 1, 2026-08-13) HALTED on a 4.8× budget breach. **Open:** `check-dispatch` records no agent identity, so the command cannot tell a named-agent dispatch from a model-pinned generic one. (Step-5 run-log gating closed 2026-08-18 — see ADR-0004) |
| [0009](0009-agent-output-snapshots.md) | Snapshots ledger — `docs/snapshots/` verbatim agent-output exhibits (claimant-report by construction, no orphans, load-bearing threshold, handoff sole writer) as the kit's third folder | **Accepted** 2026-08-18 (Proposed 08-02) | Nothing built — no folder, no `check-snapshots`, no skill edits | Ratified 2026-08-18: decision 1 resolved by the strict reading (no backfill — the folder starts empty everywhere); decisions 2–3 (run-log citations; gate placement) deferred to build time |
| [0010](0010-learn-from-misfire.md) | LEARN loop — misfire → blameless mechanism → pinned gate → checkable closure; a closure ledger with three states where `open` makes completeness unevaluable | **Accepted** 2026-08-18 | **Built same day, pre-ratification (operator-directed), since regularized**: `skills/learn-from-misfire/`, `scripts/check-misfire-closure.mjs` + 21 tests (three exit codes proven on real fixtures), `docs/learn/` ledger (empty), and the self-application audit. Ledger schema ratified with the ADR | The audit found **8 open / 0 declined / 5 pinned** — each open item needs a pin-with-red-proof or a dated decline. Sprawl review vs ADR-0009's proposed third folder |
| [0011](0011-verifier-calibration-ledger.md) | Verifier-calibration ledger — per verifier × tier × claim class precision/recall computed from an append-only log; seeded known-true/known-false controls, with a control failure demoting that session's verdicts to unevaluable | **Accepted** 2026-08-18 | **Nothing built** — no folder, no `check-verifier-calibration`, no `config/calibration.json` | Open by the ADR's own admission (and the demotion scope must be decided before the first control dispatch): selection bias makes precision-on-adjudicated ≠ precision; small-n makes any floor theatre; demotion scope (whole session vs claim class) is undecided. Control-claim cost must be priced into the L1 cap before dispatch |
| [0012](0012-re-audit-sweep.md) | Re-audit sweep — verification over standing published state as upstream reality drifts; drift-vs-rot, a drift budget, and a verified → stale → invalidated demotion ladder | **Accepted** 2026-08-18 | **Nothing built** — design only, deliberately (largest surface, sequenced last) | **Records the ADR-0005 resolution-2 unblock:** its stated precondition (ADR-0004's pilot evaluation) was discharged 2026-07-14; what remains is an explicit operator go in a run log. Cost is O(catalog) unless consumption-weighted |
| [0013](0013-deployment-layer-pre-change-authorization.md) | Deployment layer — a pre-change authorization gate in four control shapes (review / preventive / detective / evidentiary) over six properties, with a change-class rubric; rigor is the control at the change-approval step, never the deploy executor | **Proposed** 2026-08-22 (second draft; first draft refuted same day by three skeptics — two blocking findings fixed) | **Built same day, pre-ratification, all provisional:** six skills, `scripts/check-change-record.mjs` (three outcomes), `hooks/change-guard.mjs` (third hook), `hooks/shell-normalize.mjs` shared by both PreToolUse hooks, `git-guard` hardening; test floor 178 → 523; built through one build + four fix rounds, each refuted by skeptics until round 5 survived (`plans/2026-08-22-deployment-layer-build.md`, 6.1M subagent tokens); first-domain rehearsal at record level (`audits/2026-08-22-deployment-layer-first-domain-rehearsal.md`) | Ratify or reject; first domain (ATLAS kernel, Kustomize on EKS) has **zero** remote protection rules and a CD that has never succeeded — no live run possible; hook is friction, not a boundary (named residual tail); class-0 sweep cadence unmeasured; 0 of 4 review controls credited |

## Reading the statuses

- **Accepted** — the decision is locked; relitigate only if its stated *reason* stops holding
  (`pick-up` move 4 checks premises, never the decision).
- **Proposed** — a recommendation. **Never quote a Proposed ADR's resolutions as practice.**
  ADR-0005 is the live example: its mapping table reads like doctrine and is not yet ratified.
- **Settled** (ADR-0004 only) — an ADR that carried a *pilot* whose success criteria were
  evaluated and met. Distinct from Accepted: acceptance authorized the experiment, settlement
  reports its outcome.

Amendments are dated and appended in place; an ADR is never quietly rewritten. Where an amendment
changed the letter of a decision (0003's folder layout, its §7 dogfooding split, and the
capture-time anchoring rule), the amendment says which clause it revises and why.
