node_modules/
*.log
.DS_Store

# local-only surface-scrub denylist — project fingerprints never ship
surface-scrub.denylist

# local-only planning rationale — engagement fingerprints never ship
*.local.md
