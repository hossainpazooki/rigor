---
description: Start from a handoff brief without trusting it — verify its load-bearing claims, detect drift since it was written, and re-run the entry gate before building.
status: provisional
---

Invoke the `pick-up` skill on the handoff brief below (a path or a topic; if
empty, locate the newest handoff brief by the repo's convention). Apply all
five moves — read and date the brief, drift-check the repo since it was
written, refute the load-bearing state claims (using the brief's `re-verify:`
lines where present), check locked decisions' premises, and re-run the entry
gate — then produce the pick-up report before beginning any work.

Brief: $ARGUMENTS
