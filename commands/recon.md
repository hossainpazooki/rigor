---
description: Decompose a question into disjoint parallel recon, refute the findings, and synthesize an answer.
status: provisional
---

Invoke the `fanout-recon-synthesize` skill on the question below. Decompose it
into disjoint recon tasks against one shared return contract, fan them out in
parallel, refute every load-bearing finding (via `refute` / `skeptic-verifier`),
and synthesize the survivors into a conclusion — naming anything dropped or never
covered:

$ARGUMENTS
